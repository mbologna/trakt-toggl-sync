name: CI

"on":
  push:
    branches: ["**"]
    tags: ["v*.*.*"]
  pull_request:
  schedule:
    - cron: "0 6 * * 1"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write
  security-events: write

jobs:
  python:
    uses: mbologna/.github/.github/workflows/python-ci.yml@bc67bfd693eafaf8371ae6ba2626e1f977ab3311 # main
    with:
      python_version: "3.14"
      use_uv: true
      enable_codecov: true
    secrets: inherit

  docker:
    needs: python
    uses: mbologna/.github/.github/workflows/docker-ci.yml@bc67bfd693eafaf8371ae6ba2626e1f977ab3311 # main
    with:
      image_name: trakt-toggl-sync
      push_to_dockerhub: true
      enable_grype: true
      enable_cosign: true
      enable_release_drafter: true
    secrets: inherit

  auto-release:
    if: github.event_name == 'schedule'
    needs: [docker]
    uses: mbologna/.github/.github/workflows/auto-release.yml@bc67bfd693eafaf8371ae6ba2626e1f977ab3311 # main
    with:
      strategy: docker
      image: docker.io/mbologna/trakt-toggl-sync
    secrets: inherit

  deploy:
    needs: [docker]
    if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule'
    uses: mbologna/.github/.github/workflows/gcp-deploy.yml@bc67bfd693eafaf8371ae6ba2626e1f977ab3311 # main
    with:
      image_name: trakt-toggl-sync
      deploy_type: service
      deploy_name: trakt-toggl-sync
      region: us-central1
    secrets: inherit
